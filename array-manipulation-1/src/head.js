/* exported head */

// declare a single parameter named array
//  assign the value of the first element in array to a new variable named firstElement
//   return the value of the variable firstElement

function head(array) {
  var firstIndex = array[0];
  return firstIndex;
}

head([1, 3, 5]);
