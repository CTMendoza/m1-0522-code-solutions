/* exported getLastChar */
// declare new function named getFirstChar
//  declare a single parameter string
//   assign the first element of the string to a new variable
//     return the new variable

function getLastChar(string) {
  var lastStringIndex = string.length - 1;
  var lastChar = string[lastStringIndex];
  return lastChar;
}
