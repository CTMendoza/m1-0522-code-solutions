/* exported setValue */
// define new function setValue
//  declare three parameters object key and value
//    assign value to the key of the objet
function setValue(object, key, value) {
  object[key] = value;
}
