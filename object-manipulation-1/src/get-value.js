/* exported getValue */
// define new function getValue
// declare two parameters object and key
// declare a new variable called keyValue
// assign the property value of the object to keyValue
// return keyValue
function getValue(object, key) {
  var keyValue;
  keyValue = object[key];
  return keyValue;
}
